import XCTest
@testable import MomentsOfSpaceCore

final class MomentsOfSpaceCoreTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MomentsOfSpaceCore().text, "Hello, World!")
    }
}
